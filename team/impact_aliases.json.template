{
  "_doc": "impact_scope.js 개념 별칭 — **사람이 승인한 테이블이다. 크론·LLM이 스스로 추가하지 말 것.** 이 파일은 템플릿이다. `impact_aliases.json` 으로 복사해서 쓴다(없어도 impact_scope.js 는 정상 동작하고, 별칭 구제만 비활성된다).",
  "_why": "그래프 노드 제목의 토큰이 기획서 본문에 없지만 개념은 분명히 있는 경우가 있다. 토큰 매칭만 쓰면 이런 축을 통째로 놓친다.",
  "_등재_자격": "①그 노드가 hop1/hop2 후보에 실제로 있고 ②본문에 개념어가 있는데 제목 토큰만 다를 때. 유사·연상 금지 — 근거 문장을 note에 남긴다.",
  "_형식": "<그래프 노드 id>: { terms: [본문에서 찾을 개념어], note: 등재 근거 }",

  "<그래프_노드_id>": {
    "terms": ["<본문에 실제로 등장하는 개념어1>", "<개념어2>"],
    "note": "<등재 근거 — 원문 어느 절에 무슨 문장이 있어서 이 노드로 본다. 날짜 포함>"
  }
}
