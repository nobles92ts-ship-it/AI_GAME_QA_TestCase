# TC 팀 v2 — 모니터 출력 규칙 (SSoT)

팀장은 STEP 전환 시점마다 본 파일 Read → 모니터 블록 렌더링.

---

## 갱신 시점
- **STEP 전환 직후**: 이전 완료 + 다음 시작 시 1회 재출력 (권장)
- 30초 강제 주기 갱신 없음 (노이즈 방지)
- 배치 내 기능 전환 시 전체 블록 재출력

---

## 파일 출력 (PowerShell 외부 모니터용)

팀장은 모니터 블록을 **`monitor.log`에 overwrite** 저장:

```bash
MONITOR_FILE="{WORK_ROOT}/team/monitor.log"

# 모니터 블록 출력 함수 (STEP 전환마다 호출)
print_monitor() {
  cat <<EOF
TC 팀 v2 에이전트 모니터  ($(date '+%H시 %M분 %S초'))
$(generate_monitor_body)
EOF
}

# 화면 + 파일 동시 출력 (파일 덮어쓰기)
print_monitor | tee "$MONITOR_FILE"
```

> `monitor.log`에는 **최신 블록 1개만** 저장 (덮어쓰기). PowerShell 외부 창 실시간 관찰 (`monitor.bat`).

---

## 상태 기호
| 기호 | 의미 |
|---|---|
| `·` | 대기 |
| `▶` | 진행중 (현재 포커스) |
| `✓` | 완료 (+소요시간/결과 요약) |
| `─ skip` | 조건부 스킵 (STEP 3 설계수정 / STEP 6 1차수정) |
| `✗` | 실패 (+에러 사유) |
| `⟲` | 재시도 중 (retry N회) |

---

## 출력 포맷 (단일 기능 진행 중)

```
TC 팀 v2 에이전트 모니터  (HH시 MM분 SS초)
─────────────────────────────────────────────────
 기능: [기능명]  │  경과 MM:SS  │  비용 $N.NN / $30
─────────────────────────────────────────────────
 대기   │ ✓ 시작 HH:MM:SS
 설계   │ ✓ 완료 (M:SS)
 검수   │ ✓ 완료 (needs_fix=false, M:SS)
 수정   │ ─ skip
 작성   │ ▶ 진행중 (haiku, M:SS 경과)        ← 현재
 리뷰1  │ ·
 수정1  │ ·
 리뷰2  │ ·
 수정2  │ ·
 검증   │ ·
```

---

## 출력 포맷 (배치 모드, 예: 3개)

```
TC 팀 v2 에이전트 모니터  (HH시 MM분 SS초)
─────────────────────────────────────────────────
 배치 진행률: ▓▓▓▓▓▓▓░░░ 2/3  │  총 경과 HH:MM  │  누적 $N.NN / $30
─────────────────────────────────────────────────
 [1/3] ✓ [기능1]     완료 MM:SS  TC N개  $N.NN
 [2/3] ▶ [기능2]     진행중 MM:SS        $N.NN  ← 현재
 [3/3] · [기능3]     대기
─────────────────────────────────────────────────
 현재 기능: [기능2]
 대기   │ ✓ 시작 HH:MM:SS
 설계   │ ✓ M:SS
 검수   │ ✓ M:SS (needs_fix=false)
 수정   │ ─ skip
 작성   │ ✓ M:SS (TC N개)
 리뷰1  │ ▶ 진행중 (sonnet, M:SS 경과)    ← 현재 STEP
 수정1  │ ·
 리뷰2  │ ·
 수정2  │ ·
 검증   │ ·
```

---

## 배치 완료 포맷

```
TC 팀 v2 에이전트 모니터  (HH시 MM분 SS초)
─────────────────────────────────────────────────
 배치 진행률: ▓▓▓▓▓▓▓▓▓▓ 3/3 ✅  │  총 HH:MM:SS  │  누적 $N.NN / $30
─────────────────────────────────────────────────
 [1/3] ✓ [기능1]     MM:SS  TC N개  $N.NN
 [2/3] ✗ [기능2]     MM:SS  실패: [에러 사유] (STEP N)
 [3/3] ✓ [기능3]     MM:SS  TC N개  $N.NN
─────────────────────────────────────────────────
 전체: 2/3 완료  │  1 실패  │  TC 총 N개
 잔여 예산: $N.NN / $30

 ⚠️ 실패 기능 재실행 템플릿:
    /tc-v2 [스프레드시트 URL] [기능2 Confluence URL]
```

---

## 비용 임계 경고 (모니터 상단 강조)

**정지 기능 OFF**. `batch_cost_summary.sh` 항상 exit 0. 초과해도 파이프라인 계속.

```bash
bash "$UTIL/batch_cost_summary.sh"   # 항상 정보성 — 차단 X
```

모니터 상단 비용 색상 표기 (정보성):
- 잔여 $5 이하 → `비용 $25.00 / $30 🟡 (정보성)`
- $30 초과 → `비용 $31.10 / $30 [한도 초과 — 계속 진행 중]`

---

## 실패 배치 재시도 가이드

배치 완료 블록에 실패 기능 있으면 **재실행 템플릿 1줄** 자동 출력. 복사-붙여넣기로 재시도.

```
 ⚠️ 실패 기능 재실행 템플릿:
    /tc-v2 [스프레드시트 URL] [실패기능1 URL] [실패기능2 URL]
```

---

## 비용 실시간 가시화 스크립트

매 STEP 완료 직후 또는 배치 내 기능 전환 시:
```bash
bash "$UTIL/batch_cost_summary.sh"
```
- 기능별 소계 + 배치 누적 + 잔여 예산 출력
- 단일 기능 $10 초과 → 경고 (루프 의심, 정지 X)
- 배치 누적 $30 초과 → 정보성 로그 (**정지 OFF** — 계속)

---

## 외부 모니터 (pipeline_monitor.js)

`monitor.bat` 더블클릭 → `pipeline_monitor.js --watch` 실행 → PowerShell 창에서 `state.json` 기반 5초 주기 자동 렌더링.
- 규칙 변경 시 `$UTIL/pipeline_monitor.js` 소스 수정 (본 문서 포맷과 동기화 필요)
