# 인증 정보 (절대 올리지 않음)
.env
.env.local
.env.*.local
credentials/client_secret.json
# ⚠ 이름 나열이 아니라 글로브다. 개별 이름만 적어 두면 **새로 생긴 설정 파일이 무방비**가 된다 —
#   실측(2026-08-23): jira_config.json·slack_config.json 은 막혔는데 나중에 추가된
#   slack_dm_config.json 은 같은 종류의 실 토큰을 담고도 어디에도 안 걸려 있었다.
#   `.template`·`.example` 접미형은 이 글로브에 안 걸리므로 동봉본은 그대로 추적된다.
*_config.json
pipeline_config.local.json
*.token.json
*.credentials.json
.mcp.json
# Legacy Korean-named credential folder (if exists)
**/구글*클라우드*API*키값/
client_secret_*.json
*-bot-*.json
oauth_token.json

# 런타임 산출물
team/tc_config.json
team/specs/
team/state.json
team/status/
team/timing.csv
team/lessons/
team/learned_patterns.json

# 히스토리 / 로그 / 캐시
qa_history.json
*_snapshot.json
**/tc_after_fix*.json
**/_final_dump.json
**/confluence_raw.md
**/sheet_info.txt
*.err.txt
qa_opinion.txt
history.jsonl
stats-cache.json

# Node.js
node_modules/
node-v20*/

# OS
.DS_Store
Thumbs.db
.code-review-graph/
